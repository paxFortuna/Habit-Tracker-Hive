import 'package:flutter/material.dart';

class MonthSummary extends StatelessWidget {
  const MonthSummary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
